#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: Prints the specified text using the `printf` function
 *              and returns 0.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    printf("with proper grammar, but the outcome is a piece of art,\n");
    return (0);
}
